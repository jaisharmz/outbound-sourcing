-- Why a source run set a company aside. Kept as triage context, not a rejection:
-- an industry map's inclusion test is topical and answers a different question
-- than "is this a viable prospect".
ALTER TABLE accounts ADD COLUMN source_note TEXT;
